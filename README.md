# F3K Logbook

Mobile-first GitHub Pages/PWA viewer for SRK F3K flight logs.

## Updating logs
Put `index.csv` and all `f*.csv` files in the `logs/` folder, then commit to GitHub.
The app loads logs from the repository automatically.
